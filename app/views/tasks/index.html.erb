# <h1>Tasks for <%= @category.name %></h1>

# <table>
#   <thead>
#     <tr>
#       <th>Task Name</th>
#       <th>Due Date</th>
#       <th>Status</th>
#       <th></th>
#     </tr>
#   </thead>
#   <tbody>
#     <% @category.tasks.each do |task| %>
#       <tr>
#         <td><%= task.name %></td>
#         <td><%= task.due_date %></td>
#         <td><%= task.status ? 'Completed' : 'Pending' %></td>
#         <td>
#           <%= link_to 'Edit', edit_category_task_path(@category, task), class: 'btn btn-primary' %>
#           <%= button_to category_task_path(@category, task), method: :delete, data: { turbo_confirm: "Are you sure?"}, class: 'btn btn-primary' do %>
#             Delete
#             <% end %>

#         </td>
#       </tr>
#     <% end %>
#   </tbody>
# </table>

# <%= link_to 'New Task', new_category_task_path(category_id: @category.id), class: 'btn btn-success' %>
